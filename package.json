{
  "name": "tsc-project-references-demo",
  "private": true,
  "workspaces": ["packages/*"],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "typescript": "^3.7.2"
  },
  "scripts": {
    "build:cli": "(cd packages/cli && yarn build)"
  }
}
